{{# warningMessage }}  
<div class="warning-message">
  <p>This is an alert</p>
  <ul>
    <li>With a list</li>
    <li>Just because</li>
  </ul>
</div>
{{/ warningMessage }}  


<form method="post" action="{{ link.pages-dashboard }}">
  {{# formTitle }}
  <h2>{{ formTitle }}</h2>
  {{/ formTitle }}

  {{# fields}}
  {{> molecules-field }}
  {{/ fields}}

  {{> molecules-button-group}}
</form>
